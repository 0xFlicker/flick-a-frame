query Swaps($account: String!, $token: String!) {
  swapsIn: swaps(where: { account: $account, tokenIn: $token }) {
    id
    amountIn
    amountInUSD
  }

  # swapsOut: swaps(
  #   where: {
  #     account: $account
  #     tokenOut: "0x4ed4E862860beD51a9570b96d89aF5E1B0Efefed"
  #   }
  # ) {
  #   id
  #   amountOut
  #   amountOutUSD
  # }
}

query TokenPrice($token: ID!) {
  token(id: $token) {
    lastPriceUSD
    name
    symbol
  }
}
